---
description: 라운드 1만 실행합니다 (조사 → 초안 → 감사)
agent: refine-lead
---

$ARGUMENTS

요청이 비어 있으면 `sample/` 폴더의 미니 CLI 도구를 대상으로 하세요.

**Phase 0 과 라운드 1(A·B·C)만 수행하고 멈추세요.**
감사 판정이 `CONTINUE` 여도 라운드 2로 넘어가지 마세요.

마지막에 `_workspace/1-gaps.md` 를 읽고, **다음 라운드에 무엇을 조사하게 되는지** 보여 주세요.
