---
description: 이미 있는 리뷰·검증 결과로 HTML 리포트만 다시 생성합니다
agent: review-lead
---

리뷰를 다시 하지 말고 **HTML 만 다시 만드세요.**

1. `_workspace/` 에 `1-hunks.md` · `2-review-*.md` · `3-verify.md` 가 다 있는지 확인합니다.
   하나라도 없으면 **무엇이 없는지 알리고 멈추세요.** 없는 단계를 대신 수행하지 마세요.
2. `report-builder` 를 불러 `4-findings.json` 과 HTML 을 다시 생성합니다.
3. 파일이 만들어졌는지 확인하고 경로와 크기를 보고합니다.

템플릿이나 스크립트를 고친 뒤 결과를 다시 뽑을 때 쓰는 커맨드입니다.
같은 입력으로 두 번 돌리면 **같은 HTML 이 나와야 합니다.**
